import { BackgroundColor } from './background-color';

describe('BackgroundColor', () => {
  it('should create an instance', () => {
    const directive = new BackgroundColor();
    expect(directive).toBeTruthy();
  });
});
